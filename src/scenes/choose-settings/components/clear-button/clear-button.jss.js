import { colors } from "../../../../themes/colors";

const mobileStyle = {
  button: {
    color: colors.darkOrange,
    border: `${colors.darkOrange} solid 1px`,
    borderRadius: "25px",
    width: "90vw",
    height: "40px",
    margin: "auto",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    marginBottom: "10px",
    fontWeight: "bold",
  },
};

export default mobileStyle;
